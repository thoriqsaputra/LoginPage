import { Racing_Sans_One, Junge, Kotta_One, Raleway } from "next/font/google";

export const racing = Racing_Sans_One({
    weight: ['400'],
    subsets: ['latin'],
});

export const kotta = Kotta_One({
    weight: ['400'],
    subsets: ['latin'],
});

export const junge = Junge({
    weight: ['400'],
    subsets: ['latin'],
});

export const raleway = Raleway({
    weight: ['400'],
    subsets: ['latin'],
});